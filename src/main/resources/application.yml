server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sgc
    username: user
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    change-log: classpath:/migrations/changelog-master.xml
    enabled: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

app:
  storage:
    image-dir: C:/Users/Daniil/IdeaProjects/smart-garbage-collection/uploads/images

jwt:
  secret: g#C&bZ@3kTnV!qR*PxL9zFvAeHwM5UdQ
  expiration: 3600000

audit:
  templates:
    CREATE_USER: "Создан пользователь: ${request.fullName} (${request.username})"
    UPDATE_USER: "Изменён пользователь: ${userId}"
    DELETE_USER: "Удален пользователь: ${userId}"

    CREATE_COMPANY: "Создана компания: ${request.name} ИНН: ${request.inn}"
    UPDATE_COMPANY: "Изменена компаня: ${request.name} ИНН: ${request.inn}"
    DELETE_COMPANY: "Удалена компания: ${request.name} ИНН: ${request.inn}"


google:
  gemini:
    api-key: "AIzaSyCR61VsISudWV7-Z5MyXypYMJJ_OaOHqMs"
    model-name: "gemini-2.5-flash-preview-04-17"
